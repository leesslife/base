BIN文件 纯代码 没有头信息 只能load到固定的位置 ，通常适用于裸机
ELF文件 带有头部信息 用于操作系统，在目标地址没有空间时，可以根据需要load到其他地址

ELF文件
    1：可执行文件
    带有地址
    2：可重定位文件
    不带地址
    3：共享库文件
    不带地址：是重定位文件的集合归档
    动态库有入口地址  因为运行是要到指定内存地址去加载
    静态库没有入口地址